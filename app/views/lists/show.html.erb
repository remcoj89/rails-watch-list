<h2><%= @list.name %></h2>

<ul>
  <% @list.bookmarks.each do |bookmark| %>
    <li><p><%= bookmark.movie.title %>:<%= bookmark.comment %></p></li>
  <% end %>
</ul>

<p><%= link_to "add a movie", new_list_bookmark_path(@list) %> </p>
<%= link_to "back", lists_path %>
